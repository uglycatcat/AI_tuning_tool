# 创建: conda env create -f conda_env.yaml
# 更新: conda env update -f conda_env.yaml --prune
# pip 依赖与 requirements.txt 一致（路径相对本文件所在目录）。
name: ai_env
channels:
  - conda-forge
dependencies:
  - python>=3.10,<3.14
  - pip
  - pip:
      - -r requirements.txt
